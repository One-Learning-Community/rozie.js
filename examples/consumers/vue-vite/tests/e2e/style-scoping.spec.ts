// Phase 3 Wave 0 stub for VUE-05 / Phase 3 success criterion 5
// (`:root { }` rule lifts to a global <style> block; everything else stays
// in <style scoped> per D-38; Playwright visual assertion that :root rules
// apply globally while scoped rules don't leak).
// Plan 06 (P5) implements the real assertions.
import { test } from '@playwright/test';

test.skip('Wave 0 stub — Plan 06 implements VUE-05 style-scoping e2e', () => {});
