// Phase 3 Wave 0 stub for DX-01 / Phase 3 success criterion 4
// (throw runtime error from <script>; assert stack frame resolves to
// .rozie file:line:column via magic-string source maps + Vite plugin
// chain composition).
// Plan 06 (P5) implements the real assertions.
import { test } from '@playwright/test';

test.skip('Wave 0 stub — Plan 06 implements DX-01 source-map e2e', () => {});
