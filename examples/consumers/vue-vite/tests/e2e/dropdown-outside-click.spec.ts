// Phase 3 Wave 0 stub for Phase 3 success criterion 2
// (Dropdown opens, click outside closes, .outside modifier collapses with
// when: $props.open && $props.closeOnOutsideClick per D-42).
// Plan 06 (P5) implements the real assertions.
import { test } from '@playwright/test';

test.skip('Wave 0 stub — Plan 06 implements Dropdown outside-click e2e', () => {});
