---
phase: 08.1-intellij-plugin-components-block
plan: "05"
subsystem: intellij-plugin
tags: [intellij, jflex, lexer, on-update, nested-template, magic-ident, depth-counter]
dependency_graph:
  requires:
    - 08.1-02 ($onUpdate added to MAGIC_IDENT_RE; IN_TEMPLATE_ATTR_VALUE_JS_* + IN_MUSTACHE rules)
    - 08.1-03 (DIRECTIVE_COLON/DIRECTIVE_ARGUMENT_NAME — used in WrapperModal.rozie fixture's `r-model:open`)
    - 08.1-04 (SLOT_FILL_MARKER/SLOT_NAME — used in `<template #header>` / `<template #footer>` slot-fillers inside nested-template fixture)
  provides:
    - templateNestingDepth int field in Rozie.flex %{ %} block (mirrors modifierArgsParenDepth idiom)
    - IN_TEMPLATE_BODY "<template" / [\s>] rule (increments depth; emits TEMPLATE_BODY)
    - IN_TEMPLATE_BODY "</template>" decrement-and-conditional-yybegin rule (gates outer SFC close on depth==0)
    - YYINITIAL "<template" defensive reset (templateNestingDepth = 0; closes cross-file state-leak Pitfall 3)
    - edge-on-update.rozie + .txt lexer-snapshot fixture (all 6 SC-8 contexts; MAGIC_IDENT('$onUpdate') x2 host-lexer-visible + 4 opaque body contexts)
    - edge-nested-template.rozie + .txt lexer-snapshot fixture (verbatim copy of examples/WrapperModal.rozie; proves nested <template #...> does NOT prematurely close outer block)
    - testEdgeOnUpdate + testEdgeNestedTemplate RozieLexerTest methods
  affects:
    - tools/intellij-plugin (JFlex lexer + regenerated DFA + 2 new fixtures + 2 new test methods)
    - examples/WrapperModal.rozie (now lexes correctly in the IDE — premature outer-block close at the first inner </template> no longer happens)
    - tools/textmate (UNCHANGED — TM grammar's line-anchored fix at rozie.tmLanguage.json:52 already addressed this same problem TM-side)
tech_stack:
  added: []
  patterns:
    - modifierArgsParenDepth idiom (counter field in %{ %} + decrement-and-conditional-yybegin) extended to templateNestingDepth — paired-bracket-style depth tracking is now the canonical Rozie.flex pattern for "this delimiter only fires on outer-most close"
    - Defensive cross-file state reset in the YYINITIAL open-rule (FlexAdapter restores YYINITIAL on reset() but not user %{ %} fields; per-file reset MUST be either in the open-rule or in a Kotlin reset() override; option (a) chosen per RESEARCH Pitfall 3 recommendation — locally scoped, no new Kotlin code)
    - JFlex lookahead `/ [\s>]` discriminates `<template` from `<templateFoo` (a hypothetical PascalCase tag like `<TemplateFoo>`); same idiom as Rozie.flex line 181's `\" / [^\"]` lookahead
    - JFlex longest-match: "<template" (9 chars) wins over "<" (1 char) when both apply, so the new increment rule must come BEFORE the generic "<" rule for correct ordering
    - Pitfall 6 acknowledgment: 4-of-6 $onUpdate contexts (script/props/data/listeners bodies) lex as opaque *_BODY tokens; JS-side coloring rides on existing RozieInjectionTest injection-smoke coverage (no separate per-body assertion required — explicitly documented per RESEARCH Pitfall 6)
key_files:
  created:
    - tools/intellij-plugin/src/test/testData/lexer/edge-on-update.rozie
    - tools/intellij-plugin/src/test/testData/lexer/edge-on-update.rozie.txt
    - tools/intellij-plugin/src/test/testData/lexer/edge-nested-template.rozie
    - tools/intellij-plugin/src/test/testData/lexer/edge-nested-template.rozie.txt
  modified:
    - tools/intellij-plugin/src/main/jflex/Rozie.flex
    - tools/intellij-plugin/src/main/gen/js/rozie/intellij/lexer/_RozieLexer.java
    - tools/intellij-plugin/src/test/kotlin/js/rozie/intellij/RozieLexerTest.kt
decisions:
  - "Used WrapperModal.rozie verbatim as edge-nested-template.rozie fixture content. WrapperModal had no leading comments (verified — line 1 is `<rozie name=\"WrapperModal\">`), so byte-offset shifts from leading-comment stripping (feedback_rozie_leading_comments) were not a concern. The verbatim copy ensures the real-world IDE-visible bug is what the test regression-guards."
  - "Snapshot regeneration helper RE-COMMENTED in committed form, with the canonical example list extended to include BOTH new fixtures (edge-on-update.rozie + edge-nested-template.rozie). Plan 04's convention was to keep the helper commented when not actively generating; future executors inherit the full 14-fixture example list."
  - "Per Pitfall 6: did NOT add a separate JS-injection assertion for the 4 body-context $onUpdate occurrences. The lexer snapshot proves the SCRIPT_BODY/PROPS_BODY/DATA_BODY/LISTENERS_BODY tokens correctly bound the $onUpdate text; the existing injection-smoke RozieInjectionTest covers that JS injection lexes those body bodies through the JS host — $onUpdate then becomes just a JS identifier inside an injected JS file (the magic-ident TM scope colors it via the TM injection arm). Plan 05's snapshot-only validation for body contexts is the explicitly-recommended Pitfall 6 disposition."
  - "Defensive-reset OPTION (a) chosen over option (b): templateNestingDepth = 0 in the YYINITIAL `<template` rule (locally scoped, no new Kotlin code) instead of overriding reset() in RozieLexerAdapter.kt. Field is ALSO initialised to 0 at declaration (defense-in-depth — covers the very first SFC file the lexer instance sees, since the YYINITIAL <template rule has not yet fired)."
  - "JFlex lookahead `/ [\\s>]` on the new `<template` increment rule prevents PascalCase tags like `<TemplateFoo>` from being mistaken for nested templates. The lookahead character is NOT consumed (JFlex lookahead semantics) and is subsequently picked up by IN_TEMPLATE_TAG_OPEN's existing rules."
  - "JFlex longest-match means the new `<template` (9 chars) rule wins over the existing `<` (1 char) rule whenever both could match; this is the desired ordering. Rule placement in committed Rozie.flex: `<template` increment comes immediately BEFORE the generic `<` rule (rule-order is documentation-only when longest-match handles disambiguation, but the spatial ordering follows the standard Rozie.flex convention of placing more-specific literals before catch-alls)."
  - "DFA size growth: 1,160 NFA -> 374 minimised DFA (+9 over Plan 04 baseline of 365). Well under the 50-states-per-plan ceiling from Pitfall 2. Cumulative growth across Plans 02 + 03 + 04 + 05 = +22 states from the original Plan-01 baseline."
  - "Combined-plan check after Plan 05 lands: full plugin test suite is 29/29 GREEN (RozieLexerTest 14, RozieInjectionTest 9, RozieSyntaxHighlighterTest 3, TextMateGrammarParityTest 3 — all GREEN incl. D-07 parity from Plan 04). SC-8a and SC-8b acceptance criteria both closed."
  - "Inner `<template ...>` open emits as a single TEMPLATE_BODY('<template') token rather than the regular tag pattern (TEMPLATE_BODY('<') + ATTR_NAME('template')). This is a deliberate consequence of using a single literal `<template` match for the increment rule (per RESEARCH Example 3 verbatim); the slot-fill machinery (`#header`, `#[...]`) is the user-visible surface, not the tag name, so the snapshot tokenisation is asymmetric but semantically correct. Cross-checked against Plan 04's edge-slot-fill snapshot for `<Modal>` parents — those still emit TEMPLATE_BODY('<') + COMPONENT_REF('Modal') because the new rule fires only on the literal `<template` prefix."
  - "Sub-repos: N/A — Rozie is a single-repo monorepo; standard `git commit` used."
metrics:
  duration: "~4 minutes"
  completed_date: "2026-05-17"
  tasks_completed: 2
  commits:
    - 6839773 — test(08.1-05): edge-on-update.rozie fixture covering $onUpdate 6 contexts
    - 83c597b — feat(08.1-05): templateNestingDepth counter fixes nested-template early-close bug
---

# Phase 08.1 Plan 05: $onUpdate 6-context coverage + templateNestingDepth nested-template fix Summary

One-liner: Adds the templateNestingDepth counter to Rozie.flex (mirroring the existing modifierArgsParenDepth idiom) so nested `<template #slotName>` slot-fillers inside an outer `<template>` SFC block no longer prematurely terminate the outer block at the first inner `</template>` — combined with a fixture-only validation that `$onUpdate` lexes as MAGIC_IDENT in the 2 host-lexer-visible contexts (template-attr expression + `{{ }}` mustache) while surfacing inside opaque *_BODY tokens in the 4 JS-block contexts where JS-injection coloring takes over.

## What Got Built

| Component | Status | Notes |
|-----------|--------|-------|
| `templateNestingDepth` int field in `Rozie.flex` `%{ %}` block | shipped | Mirrors `modifierArgsParenDepth` idiom (lines 88-92); docstring documents the cross-file reset rationale (Pitfall 3) |
| IN_TEMPLATE_BODY `"</template>"` decrement-and-conditional-yybegin rule | shipped | Outer SFC close only on `depth==0`; inner close decrements and returns TEMPLATE_BODY (state preserved) |
| IN_TEMPLATE_BODY `"<template" / [\s>]` increment rule | shipped | Lookahead `[\s>]` discriminates from `<TemplateFoo` PascalCase; placed BEFORE generic `<` rule (JFlex longest-match) |
| YYINITIAL `"<template"` defensive reset (`templateNestingDepth = 0`) | shipped | Per-file cross-state-leak mitigation (RESEARCH Pitfall 3 option (a) — locally scoped, no new Kotlin code) |
| `_RozieLexer.java` regenerated | shipped | DFA 365 → 374 states (+9, well under 50/plan ceiling from Pitfall 2); 0 "Rule can never be matched" warnings; Pitfall 1 co-committed alongside `.flex` |
| `edge-on-update.rozie` + `.txt` snapshot | shipped | All 6 SC-8a contexts: 2 host-lexer-visible (`MAGIC_IDENT('$onUpdate')` x2) + 4 opaque body contexts (Pitfall 6 acknowledgment) |
| `edge-nested-template.rozie` + `.txt` snapshot | shipped | Verbatim copy of `examples/WrapperModal.rozie`; snapshot shows EXACTLY 1 `TEMPLATE_CLOSE_TAG` (outer SFC close); 2 inner `</template>` tags emit `TEMPLATE_BODY` |
| `testEdgeOnUpdate()` + `testEdgeNestedTemplate()` RozieLexerTest methods | shipped | Integrated into standard fixture-snapshot runner |
| `testRegenerateSnapshots` helper RE-COMMENTED | shipped | Example fixture list extended with both new fixtures (now 14 total) for future executors |

## Verification Run

```
cd tools/intellij-plugin && ./gradlew test
=> BUILD SUCCESSFUL — 29/29 passing across all 4 test suites:
   - RozieLexerTest:                14 tests, 0 failures (testEdgeOnUpdate + testEdgeNestedTemplate added)
   - RozieInjectionTest:             9 tests, 0 failures (unchanged — body-context $onUpdate validation rides on existing injection-smoke per Pitfall 6)
   - RozieSyntaxHighlighterTest:     3 tests, 0 failures (unchanged)
   - TextMateGrammarParityTest:      3 tests, 0 failures (unchanged — D-07 parity remains fully GREEN from Plan 04)

cd tools/intellij-plugin && ./gradlew generateRozieLexer compileKotlin compileJava
   => OK NFA 1,160 -> DFA 374 (+9 over Plan 04 baseline of 365), zero "Rule can never be matched" warnings

git diff --exit-code -- tools/textmate/syntaxes/rozie.tmLanguage.json
   => OK unchanged (TM grammar's existing line-anchored fix at rozie.tmLanguage.json:52 already addressed the bug TM-side)

Snapshot inspection (tools/intellij-plugin/src/test/testData/lexer/edge-on-update.rozie.txt):
   - MAGIC_IDENT ('$onUpdate') occurrences: 2 (template-attr + mustache)
   - SCRIPT_BODY / PROPS_BODY / DATA_BODY / LISTENERS_BODY tokens present and structurally correct
   - $onUpdate inside body tokens visible as text fragments (lexer correctly does NOT descend; JS-injection arm colors them)

Snapshot inspection (tools/intellij-plugin/src/test/testData/lexer/edge-nested-template.rozie.txt):
   - TEMPLATE_CLOSE_TAG occurrences: EXACTLY 1 (the outer SFC close at line 104)
   - 2 inner </template> tags (lines 74, 98) emit TEMPLATE_BODY('</template>') — outer state preserved
   - 2 inner <template open tags (lines 42, 81) emit TEMPLATE_BODY('<template') — increment rule fired
   - <Modal>, <slot>, <h2>, slot-fill markers (#header, #footer), r-model:open, $props all lex correctly throughout

grep -c "templateNestingDepth" tools/intellij-plugin/src/main/jflex/Rozie.flex => 5 (>= 4 required)
grep -c "templateNestingDepth" tools/intellij-plugin/src/main/gen/js/rozie/intellij/lexer/_RozieLexer.java => 5 (field + 1 YYINITIAL reset + 1 increment + 2 in decrement-check)
```

## Deviations from Plan

None — both tasks executed exactly as written. The single judgment call:
- The plan permitted stripping leading comments from `WrapperModal.rozie` if any existed (per `feedback_rozie_leading_comments`); verification showed `WrapperModal.rozie` has no leading comments, so a verbatim copy was used.

## Authentication Gates

None encountered.

## Known Stubs

None — every new component has a real implementation and a passing test.

## Threat Flags

None — the new surface (1 new JFlex `%{ %}` field + 3 rule edits + 2 new fixtures + 2 new test methods) is defensive by construction:

- JFlex DFA has no backtracking → zero ReDoS surface.
- Counter overflow risk bounded by file size: int max is 2.1B, well beyond any realistic source file's nesting depth (T-08.1-05-02 accepted disposition stands).
- Cross-file state-leak mitigation in place via YYINITIAL defensive reset (T-08.1-05-03 mitigated per RESEARCH Pitfall 3 option (a)).
- `_RozieLexer.java` regenerated and committed alongside `.flex` (T-08.1-05-01 mitigated; existing CI `lexer-nodiff` job would fail if a future PR regresses).
- IDE plugin operates on local `.rozie` files only — no network, auth, or untrusted-input handling.

## TDD Gate Compliance

Plan 05 is `type: execute` (not `type: tdd`). No RED/GREEN gate sequence required. Fixture-snapshot test pattern is the established convention for JFlex lexer plans in this phase (per Plans 01 + 02 + 03 + 04).

## Hand-off to Plan 06

Plan 05 closes SC-8a and SC-8b. Phase 08.1 now has:
- Plans 01-02: components-block + core JFlex/highlighter/injection scaffolding
- Plan 03: DIRECTIVE_COLON + DIRECTIVE_ARGUMENT_NAME (`r-model:open` form)
- Plan 04: SLOT_FILL_MARKER + SLOT_NAME + SLOT_DYNAMIC_BRACKET_OPEN/CLOSE (4 slot-fill variants + D-07 fully GREEN)
- Plan 05: templateNestingDepth (nested-template bug fixed) + $onUpdate fixture coverage

Plan 06 (human UAT) should now find:
- `examples/WrapperModal.rozie` lexes correctly in-IDE: outer `<template>` no longer collapses at the first inner `</template>`
- `$onUpdate` carries MAGIC_IDENT coloring in template attribute expressions and `{{ }}` mustaches; in `<script>` / `<props>` / `<data>` / `<listeners>` bodies it inherits its JS-side coloring from the standard JS injection (no Rozie-host highlighting layer is involved in those bodies)

## Self-Check: PASSED

Verified post-write:
- `tools/intellij-plugin/src/main/jflex/Rozie.flex` exists; contains `templateNestingDepth` 5x (field decl + YYINITIAL reset + 2 in decrement-check + 1 in increment); modifierArgsParenDepth idiom intact at lines 88-92, 524-537; Plan 03/04 rules (R_DIRECTIVE_RE, slot-fill, IN_DIRECTIVE_ARG_*, IN_AFTER_SLOT_FILL_MARKER, IN_SLOT_BRACKET_EXPR) untouched.
- `tools/intellij-plugin/src/main/gen/js/rozie/intellij/lexer/_RozieLexer.java` exists; contains `templateNestingDepth` 5x at expected sites; regenerated cleanly (NFA 1160 → DFA 374); CI `lexer-nodiff` would pass (file committed alongside .flex).
- `tools/intellij-plugin/src/test/testData/lexer/edge-on-update.rozie` exists with RESEARCH Pattern 5 content (no leading comments per feedback_rozie_leading_comments); `.txt` snapshot has MAGIC_IDENT('$onUpdate') exactly 2x (template-attr + mustache) plus the 4 opaque body tokens.
- `tools/intellij-plugin/src/test/testData/lexer/edge-nested-template.rozie` exists as verbatim copy of `examples/WrapperModal.rozie`; `.txt` snapshot has EXACTLY 1 `TEMPLATE_CLOSE_TAG` occurrence (line 104; the outer SFC close) — inner `</template>` tags emit `TEMPLATE_BODY('</template>')` instead.
- `tools/intellij-plugin/src/test/kotlin/js/rozie/intellij/RozieLexerTest.kt` has `testEdgeOnUpdate()` + `testEdgeNestedTemplate()` methods; `testRegenerateSnapshots` helper is RE-COMMENTED with both new fixtures added to its example list (now 14 fixtures total).
- Commits exist on worktree branch `worktree-agent-a3f9b7c7592172093`: `6839773` (Task 1) + `83c597b` (Task 2) — verified via `git log --oneline -3` which shows them immediately atop the prior phase commit `039c3e4` (Plan 04 close).
- Full plugin test suite: 29/29 passing across RozieLexerTest (14), RozieInjectionTest (9), RozieSyntaxHighlighterTest (3), TextMateGrammarParityTest (3).
- TM grammar (`tools/textmate/syntaxes/rozie.tmLanguage.json`) UNCHANGED (existing line-anchored fix already addresses the bug TM-side).
