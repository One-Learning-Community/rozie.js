---
phase: 07.3.2
plan: 05
subsystem: visual-regression/d-04-acceptance-gate
tags: [visual-regression, sc#5, d-04, task-1-complete, task-2-complete, surface-followups]
requires:
  - 07.3.2-01 (React producer-side slots intake)
  - 07.3.2-02 (Solid producer-side slots intake)
  - 07.3.2-03 (Angular producer-side templates intake)
  - 07.3.2-04 (React WrapperModal re-projection root-cause fix)
provides:
  - D-04 functional acceptance gate spec: 12 new VR cells (6 targets × 2 tests) inside the existing 6-cell `for (const target of TARGETS)` loop in `tests/visual-regression/specs/modal-consumer-close.spec.ts` (Task 1 — landed 2026-05-17 at commit bce9b99)
  - PNG baseline byte-identity verification — pinned Playwright Docker recipe (`mcr.microsoft.com/playwright:v1.60.0-jammy`) re-run end-to-end on 2026-05-17 produced ZERO byte deltas in `tests/visual-regression/__screenshots__/*.png` vs. committed baselines from c877569 (2026-05-16). Existing baselines are correct.
  - Cleanup hardening — `tests/visual-regression/scripts/build-cells.mjs` `cleanupCrossTreeAngularArtifacts()` now globs `examples/*.rozie.ts` and `examples/demos/*.rozie.ts` instead of relying on hand-maintained `REFERENCE_BASENAMES`. This unblocked Task 2 and prevents future fixture additions from re-breaking the matrix the same way.
  - Honest surfacing of 4 surviving D-04 functional regressions in React/Solid/Angular dynamic-fill (`.dynamic-fill` element absent from DOM) + React Modal 3 WrapperModal re-projection (`Re-projected brand` / `Wrapper action` absent). These are producer-side emitter gaps in Plans 01-04 implementations — Plan 05 surfaces them via the D-04 acceptance gate but does NOT fix them (out of scope).
deferred:
  - Producer-side D-04 functional bugs in React/Solid/Angular targets (4 cells fail the new D-04 dynamic-fill + React re-projection assertions). New hotfix plan candidate filed below as F-07.3.2-05-A. NOT touching emitter source in Plan 05.
affects:
  - tests/visual-regression/specs/modal-consumer-close.spec.ts (Task 1 — committed bce9b99)
  - tests/visual-regression/scripts/build-cells.mjs (Task 2 unblock — committed 0fba4ad)
  - tests/visual-regression/__screenshots__/*.png (Task 2 verified — byte-identical, no changes committed)
  - Phase 07.3.2-06 HUMAN-UAT plan: gate now depends on F-07.3.2-05-A landing (the 4 producer-side D-04 functional bugs), NOT on PNG baseline drift (which is already resolved)
  - Phase 07.3.2 SC#5: PARTIAL acceptance — D-04 spec landed + PNG baselines verified Linux-current; D-04 functional pass-rate is 2/6 (vue+svelte+lit pass dynamic-fill+reproj; react+solid+angular fail). NOT yet fully closed.
tech-stack:
  patterns:
    - "D-04b same-file extend precedent — 2 new `runner(...)` blocks INSIDE the existing 6-cell `for (const target of TARGETS)` loop in modal-consumer-close.spec.ts; reuses `built` gating; matches Phase 07.3.1 peer-phase pattern"
    - "Orthogonal gating — D-04 functional tests gate on `built` only, NOT on TARGETS_WHERE_CLOSE_PROPAGATES (close-propagation is irrelevant to dynamic-fill + re-projection assertions)"
    - "Cleanup-by-glob over hand-maintained-list — replaced REFERENCE_BASENAMES iteration with readdirSync over examples/ and examples/demos/, preventing per-fixture-addition cleanup-list bitrot"
    - "Docker baseline regen workflow — pinned `mcr.microsoft.com/playwright:v1.60.0-jammy` image + `--memory=8g` flag (transient ENOMEM observed on first attempt without explicit allocation) + `CI=true pnpm install --frozen-lockfile` on host post-Docker to restore darwin native bindings"
key-files:
  modified:
    - tests/visual-regression/specs/modal-consumer-close.spec.ts (Task 1, +54 lines, 2 new runner blocks — committed bce9b99 on 2026-05-17)
    - tests/visual-regression/scripts/build-cells.mjs (Task 2 unblock, +32 -5 lines — committed 0fba4ad on 2026-05-17)
  not_modified_because_byte_identical:
    - tests/visual-regression/__screenshots__/ModalConsumer-react.png (Linux baseline at c877569 is byte-identical to fresh Docker render)
    - tests/visual-regression/__screenshots__/ModalConsumer-solid.png (ditto)
    - tests/visual-regression/__screenshots__/ModalConsumer-angular.png (ditto)
    - tests/visual-regression/__screenshots__/ModalConsumer-vue.png (ditto, including post `-u` regen)
    - tests/visual-regression/__screenshots__/ModalConsumer-svelte.png (ditto)
    - tests/visual-regression/__screenshots__/ModalConsumer-lit.png (ditto)
decisions:
  - "Task 1 (spec extension) landed atomically at commit bce9b99 on 2026-05-17 — see prior SUMMARY revision for full detail; spec is unchanged in this rewrite."
  - "Task 2 (PNG baseline re-bless) now technically complete with ZERO baseline byte changes. The pinned `mcr.microsoft.com/playwright:v1.60.0-jammy` Docker recipe ran end-to-end successfully after two upstream blockers landed on main (dd1c4a8 Angular emitter backtick-escape + d46f597 Angular dynamic-slot computed-key) and after the in-this-plan `build-cells.mjs` cleanup glob fix. Fresh Linux-rendered PNGs are byte-identical to committed baselines, so no re-bless commit is needed — the matrix is already in the correct state."
  - "Surfaced 4 surviving D-04 functional regressions honestly rather than silently re-blessing or papering over. The new D-04 acceptance gate (Task 1) successfully detects: (1) `.dynamic-fill` element absent from DOM in React/Solid/Angular ModalConsumer renders; (2) `Re-projected brand` + `Wrapper action` absent from React's Modal 3 WrapperModal render. These are producer-side emitter gaps in Plans 01-04 — the spec is correct, the bugs are real, Plan 05 surfaces them but does NOT fix them. Per `feedback_no_false_equivalent_options.md`, the honest recommendation is a follow-up hotfix plan (F-07.3.2-05-A below) rather than absorbing producer fixes into the acceptance-gate plan."
  - "Did NOT auto-fix the 4 producer-side D-04 functional regressions (Rule 4 applies — fixing dynamic-name slot bridge + render-prop named-slot invocation across 3 target emitters is architectural-scope work, not a Rule 1/2/3 inline auto-fix). Rule 4 calls for surfacing to user/orchestrator for a separate plan decision."
  - "Did fix Rule 3 (Blocking) issue: `build-cells.mjs` `cleanupCrossTreeAngularArtifacts()` was missing `Table.rozie.ts` from its hand-maintained list, causing the lit sub-build to fail in the Docker recipe. The fix is in-scope per Rule 3 (DIRECTLY blocked Task 2 from running) AND aligns with the prior SUMMARY's deferred item D-07.3.2-05-B. Used the glob-pattern approach to make the cleanup forward-compatible with future fixture/demo additions."
  - "Did NOT delete the prior SUMMARY's blocker documentation — instead replaced this SUMMARY's body with the post-Task-2 state. Git history (commit 69a4d90) preserves the partial-delivery narrative for the period 2026-05-17 spec-extension-only landing through 2026-05-17 Task-2 completion."
metrics:
  duration: "~40 minutes (Task 2 only — Docker recipe attempts + cleanup fix + verification)"
  completed: "2026-05-17"
  tasks_completed_in_full: 2
  tasks_partially_completed: 0
  tasks_deferred: 0
  surfaced_followups: 1
  commits: 2
  files_modified: 2
  files_deferred: 0
---

# Phase 07.3.2 Plan 05: D-04 Spec Extension + Linux PNG Baseline Verification — Both Tasks Now Complete; 4 Producer-Side D-04 Regressions Surfaced

**One-liner:** Task 1 (spec extension, commit bce9b99 on 2026-05-17) was already on main; Task 2 (Linux PNG baseline regen via pinned Playwright Docker) now also complete — fresh Linux render is byte-identical to committed baselines from c877569, so no re-bless commit needed. The new D-04 acceptance gate successfully surfaces 4 surviving producer-side functional regressions in React/Solid/Angular dynamic-fill + React WrapperModal re-projection; those are filed for a follow-up hotfix plan and are out of Plan 05's scope.

## What Landed

### Task 1: Spec extension — COMPLETE (prior commit bce9b99)

Task 1 landed on 2026-05-17 at commit `bce9b99` — extended `tests/visual-regression/specs/modal-consumer-close.spec.ts` with 2 new `runner(...)` test blocks INSIDE the existing 6-cell `for (const target of TARGETS)` loop. Full detail preserved in commit log; not duplicated here.

Total: **12 new VR cells** (6 targets × 2 tests). Both new blocks gate on `built` only (orthogonal to `TARGETS_WHERE_CLOSE_PROPAGATES`).

### Task 2: PNG baseline re-bless via pinned Playwright Docker — COMPLETE (no diff to bless)

**Recipe used** (verbatim from `DEBUG.md:283-321`):

```sh
docker pull mcr.microsoft.com/playwright:v1.60.0-jammy

docker run --rm --memory=8g --memory-swap=8g \
  -v "$PWD":/workspace -w /workspace \
  mcr.microsoft.com/playwright:v1.60.0-jammy bash -c '
    npm i -g pnpm@10 > /dev/null 2>&1 &&
    CI=true pnpm install --frozen-lockfile --prefer-offline &&
    pnpm turbo run build &&
    cd tests/visual-regression &&
    pnpm exec playwright test --grep "· vue" -u &&
    pnpm exec playwright test
  '
```

(`--memory=8g` added beyond the DEBUG.md baseline because an initial attempt hit transient `ENOMEM` during `pnpm install` inside the container; the explicit allocation made it deterministic.)

**Outcome:**

| Phase                                        | Result                                                                                                                                |
|----------------------------------------------|---------------------------------------------------------------------------------------------------------------------------------------|
| Docker pull                                  | OK (image was warm in local cache)                                                                                                    |
| `pnpm install` (in-container)                | OK (19 s; failed once with ENOMEM, succeeded with `--memory=8g`)                                                                       |
| `pnpm turbo run build` (in-container)        | OK 24/24 tasks after the `build-cells.mjs` cleanup-glob fix (committed in this plan as 0fba4ad). Before the fix, lit sub-build broke on a stale `examples/Table.rozie.ts` (Angular cross-tree artefact). |
| `playwright test --grep "· vue" -u` (regen)  | OK 12 passed (10.3 s)                                                                                                                  |
| `playwright test` (full matrix verify)       | 67 passed / 5 failed / 7 skipped — 5 failures are NOT baseline drift (zero PNG byte changes after `-u` regen) but real D-04 functional gaps in producer-side emitters. See "Surviving D-04 Functional Regressions" below. |
| `git status tests/visual-regression/__screenshots__` | EMPTY — fresh Linux render is byte-identical to existing committed baselines from c877569 (2026-05-16). The matrix is in the correct Linux-baseline state already. |
| Host repair post-Docker                      | OK — `CI=true pnpm install --frozen-lockfile` restored darwin native bindings (`@rolldown/binding-darwin-arm64` MODULE_NOT_FOUND was confirmed pre-repair, GREEN turbo build post-repair).                                          |

**Conclusion:** Task 2's PNG re-bless target produces zero net diff — the matrix was already correctly Linux-baselined at c877569 (2026-05-16) before the upstream Angular emitter bugs (dd1c4a8, d46f597) were introduced and fixed. No baseline commit is necessary. The Docker recipe is verified working end-to-end.

## Surviving D-04 Functional Regressions (Surfaced, NOT Fixed)

The new D-04 acceptance gate (Task 1) detects 4 producer-side functional bugs in 3 target emitters. These are surfaced honestly — Plan 05 deliberately does NOT fix them per scope boundary:

| #  | Cell                                               | Failure                                                                                          |
|----|----------------------------------------------------|--------------------------------------------------------------------------------------------------|
| 1  | `ModalConsumer · react: matrix.spec.ts:97:5`       | PNG diff (likely follow-on of bug #2 — `.dynamic-fill` absent changes the rendered Modal 2 header) |
| 2  | `ModalConsumer · react: Modal 2 dynamic-fill`      | `.dynamic-fill` element absent from DOM (target=react) — Plan 01 producer-side slots intake gap   |
| 3  | `ModalConsumer · react: Modal 3 WrapperModal …`    | `Re-projected brand` + `Wrapper action` absent from DOM (target=react) — Plan 04 reproj gap        |
| 4  | `ModalConsumer · angular: Modal 2 dynamic-fill`    | `.dynamic-fill` element absent from DOM (target=angular) — Plan 03 templates intake gap            |
| 5  | `ModalConsumer · solid: Modal 2 dynamic-fill`      | `.dynamic-fill` element absent from DOM (target=solid) — Plan 02 producer-side slots intake gap    |

**Passing cells (confirming the gate works):**
- vue: dynamic-fill PASS, re-projection PASS
- svelte: dynamic-fill PASS, re-projection PASS
- lit: dynamic-fill PASS, re-projection PASS (D-LT cells skipped per existing `built` gate but the 2 D-04 cells gated only on `built` — Lit pass)
- angular: re-projection PASS (Angular's templates merge already handles re-projection correctly; only dynamic-fill is broken)
- solid: re-projection PASS (same story)

**Why this matters:** The new D-04 gate is doing its job — it surfaces the exact regressions Plans 01-04 were supposed to fix. Vue/Svelte/Lit work because their native slot resolution handled this pre-Phase-07.3.2. React/Solid/Angular need additional producer-side emitter work that isn't in Plan 05.

## Commits

| # | Hash       | Subject                                                                                          | Files | Lines |
|---|------------|--------------------------------------------------------------------------------------------------|-------|-------|
| 1 | `bce9b99`  | `test(07.3.2-05): extend modal-consumer-close spec with Modal 2/3 D-04 assertions` (Task 1)     | 1     | +54   |
| 2 | `0fba4ad`  | `fix(07.3.2-05): glob examples/*.rozie.ts for Angular cross-tree cleanup` (Task 2 unblock)      | 1     | +32 -5 |

(Plus this SUMMARY rewrite as the trailing docs commit.)

## Previously Documented Blockers — RESOLVED

### B-07.3.2-05-01 — Angular emitter backtick-escape bug — FIXED

**Resolved on main at commit `dd1c4a8`** (`fix(angular): escape backticks and ${ in @Component template/styles bodies`). Unit test coverage added in `packages/targets/angular/src/__tests__/`. Plan 05 no longer hits this in the Docker recipe.

### B-07.3.2-05-02 — Angular dynamic-slot computed-key syntax bug — FIXED

**Resolved on main at commit `d46f597`** (`fix(angular): dynamic-slot computed-key uses prefixThis rewrite for class-body getter`). Unit test coverage added in `packages/targets/angular/src/__tests__/`. Confirmed via `pnpm --filter angular-analogjs-demo build` — 2.04 s GREEN on host.

### D-07.3.2-05-A — Angular emitter backtick-escape (DUPLICATE) — FIXED

Same as B-07.3.2-05-01 above. Resolved in dd1c4a8.

### D-07.3.2-05-B — Cleanup function coverage for TableDemo + DropdownDemo — FIXED

**Resolved in this plan at commit `0fba4ad`** (cleanup-by-glob over examples/ + examples/demos/ replaces the hand-maintained REFERENCE_BASENAMES iteration). Future fixture/demo additions will auto-clean.

## Follow-Up Work (Hotfix Plan Candidate)

### F-07.3.2-05-A — Producer-side D-04 functional regressions in React/Solid/Angular

**Severity:** HIGH (blocks SC#5 full closure; blocks Phase 07.3.2-06 HUMAN-UAT)

**Scope:** 4 failing D-04 cells across 3 target emitters need producer-side fixes:

| Target  | Failure                                                                                       | Plan to revisit                                                                                                                           |
|---------|-----------------------------------------------------------------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------|
| React   | `.dynamic-fill` absent for Modal 2 dynamic-name slot                                          | Plan 07.3.2-01 (React producer-side slots intake) — the `slots?:` merge landed but the dynamic-name lookup `$slots[\`header${$data.slotName}\`]` is not resolving against the merged slots map at runtime. |
| React   | `Re-projected brand` + `Wrapper action` absent for Modal 3 via WrapperModal                    | Plan 07.3.2-04 (React WrapperModal re-projection) — the producer-side `?.()` no-params invocation form (emitSlotInvocation.ts:279-303) was supposed to fix this; it works for simple re-projection but not through the WrapperModal layer composing into Modal. Suggests the `(props.renderBrand ?? props.slots?.['brand'])` merge from Plan 01 isn't being threaded through nested wrapper layers in React. |
| Solid   | `.dynamic-fill` absent for Modal 2 dynamic-name slot                                          | Plan 07.3.2-02 (Solid producer-side slots intake) — same class of dynamic-name resolution bug as React.                                   |
| Angular | `.dynamic-fill` absent for Modal 2 dynamic-name slot                                          | Plan 07.3.2-03 (Angular templates intake) — Angular's reproj for Modal 3 passes (templates merge OK), but the `<ng-container *ngTemplateOutlet="templates[\`header${slotName()}\`]">` pattern (computed-key bracket access into the merged templates map) isn't resolving the dynamic key at runtime. Likely needs the prefixThis rewrite from d46f597 extended to consumer-side dynamic-name slot bridges, not just class-body getters. |

**Recommended path forward:** Open a new `/gsd-plan-phase` candidate for Phase 07.3.3 (or as `Hotfix 07.3.2-05a-react-solid-angular-dynamic-name-rerun`) with one task per target emitter + unit tests + dist-parity fixture re-bless. The D-04 acceptance gate (Plan 05 Task 1) will catch the fix when it lands — the spec is already in place and gating on the right things.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 — Blocking] Globbed examples/*.rozie.ts cleanup in build-cells.mjs**

- **Found during:** Task 2 first Docker attempt — lit sub-build broke with `Rollup failed to resolve import "lit" from "/workspace/examples/Table.rozie.ts"` (Angular cross-tree leftover from prior failed run).
- **Why blocking:** Task 2 (run pinned Playwright Docker recipe) literally could not complete the matrix verify with a broken sub-build.
- **Fix:** Replaced hand-maintained `REFERENCE_BASENAMES` iteration in `cleanupCrossTreeAngularArtifacts()` with `readdirSync(EXAMPLES_DIR).filter(e => e.endsWith('.rozie.ts'))`; same glob applied to `examples/demos/`. Added `Table.ts` to the cross-rozie shim cleanup list.
- **Files modified:** `tests/visual-regression/scripts/build-cells.mjs`
- **Commit:** `0fba4ad`
- **Rationale for in-scope:** Rule 3 says "Auto-fix blocking issues" — this DIRECTLY blocked Task 2 from running. Also aligns with prior SUMMARY's deferred item D-07.3.2-05-B and the cleanup function's own design intent (it already exists to handle exactly this class of issue).

### NOT Auto-Fixed (Surfaced for Follow-Up)

**1. [Rule 4 — Architectural] 4 producer-side D-04 functional regressions in React/Solid/Angular**

- **Found during:** Task 2 full-matrix verify
- **Why deferred:** Rule 4 applies — fixing dynamic-name slot bridge resolution + render-prop named-slot composition in 3 different target emitters is substantial cross-target work in a different scope domain than Plan 05's "acceptance gate" purpose. Each fix needs unit tests + dist-parity fixture re-bless in its own commit. Bundling all four into Plan 05 violates the scope boundary AND would deliver fixes without proper isolation.
- **Action:** Filed as F-07.3.2-05-A above. User decides whether to spin up a hotfix plan or roll into Phase 07.3.3.

### Authentication Gates

None.

## Threat Mitigations Honored

| Threat ID    | Mitigation                                                                                                                                              | Status                                                                                                                                                                                                                                                                              |
|--------------|---------------------------------------------------------------------------------------------------------------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| T-07.3.2-20  | Tampering (silent macOS baselines)                                                                                                                      | Honored — Task 2 ran the pinned `mcr.microsoft.com/playwright:v1.60.0-jammy` image per `DEBUG.md:283-321`. Fresh Linux render produced ZERO byte changes vs. committed baselines at c877569 — confirms the existing baselines are correctly Linux-rendered.                            |
| T-07.3.2-21  | Tampering (stale `@rozie/unplugin/dist/`)                                                                                                              | Honored — `pnpm turbo run build` ran fresh in the Docker container (24/24 tasks); host turbo build also GREEN after `pnpm install --frozen-lockfile` restored darwin bindings.                                                                                                       |
| T-07.3.2-22  | DoS (broken host node_modules post-Docker)                                                                                                              | Honored — `CI=true pnpm install --frozen-lockfile` ran on host after Docker. Verified via `turbo run build --filter @rozie/visual-regression --force` → 6/6 sub-builds GREEN locally.                                                                                                 |
| T-07.3.2-23  | Tampering (non-ModalConsumer baseline drift)                                                                                                            | Honored — `git diff --stat tests/visual-regression/__screenshots__/` is EMPTY after Docker run. No baseline file was touched (zero byte changes anywhere in `__screenshots__/`).                                                                                                       |
| T-07.3.2-24  | Repudiation ("agent claimed tests pass without running them")                                                                                            | Honored — this SUMMARY explicitly documents Task 2's actual pass/fail breakdown (67 passed / 5 failed / 7 skipped) and identifies which failures are real product bugs (the 5 D-04 functional cells) vs. baseline drift (none). No false claim of all-green.                          |
| T-07.3.2-SC  | npm/pip/cargo installs (Docker installs `pnpm@10`)                                                                                                      | Honored — `pnpm@10` is the workspace-pinned version (per `packageManager` field). No alternative installs attempted.                                                                                                                                                                  |

## Threat Flags

None new this plan. The prior SUMMARY's `template-literal-injection` flag for `emitDecorator.ts:156` is RESOLVED by upstream commit `dd1c4a8`.

## Known Stubs

None introduced by Plan 05. The 4 surviving D-04 functional regressions are tracked product bugs (filed as F-07.3.2-05-A above), not stubs.

## Composition with Downstream Plans

### Plan 07.3.2-06 (Wave 4 — HUMAN-UAT)

**Gate status updated:** No longer blocked on Plan 05's PNG re-bless (Task 2 verified — baselines correct) and no longer blocked on upstream Angular emitter bugs (both resolved on main). NOW blocked on F-07.3.2-05-A landing (the 4 producer-side D-04 functional regressions in React/Solid/Angular). HUMAN-UAT would currently fail visual inspection for the React/Solid/Angular dynamic-fill case and React re-projection case.

### Phase 07.3.2 SC#5 closure

**Partial → improved status:**
- D-04 spec extension (acceptance gate): LANDED (Task 1).
- D-04a PNG baseline matrix: LINUX-CORRECT (Task 2 verified zero drift).
- D-04 functional pass-rate: 2/6 (vue+svelte+lit pass; react+solid+angular fail). Surfaced as F-07.3.2-05-A.

SC#5 cannot be marked fully complete until F-07.3.2-05-A lands. But Plan 05's scope is DONE — both tasks delivered or verified, no work remaining within Plan 05's purpose.

## Self-Check: PASSED

### Files modified — verified exist

```
[FOUND]   tests/visual-regression/specs/modal-consumer-close.spec.ts (Task 1 — committed bce9b99 on 2026-05-17)
[FOUND]   tests/visual-regression/scripts/build-cells.mjs (Task 2 unblock — committed 0fba4ad on 2026-05-17)
[VERIFIED-IDENTICAL]   tests/visual-regression/__screenshots__/ModalConsumer-{react,solid,angular,vue,svelte,lit}.png (Linux baselines at c877569 are byte-identical to fresh Docker render)
```

### Commits — verified in git log

```
[FOUND]   bce9b99 test(07.3.2-05): extend modal-consumer-close spec with Modal 2/3 D-04 assertions (Task 1, 2026-05-17, prior agent)
[FOUND]   0fba4ad fix(07.3.2-05): glob examples/*.rozie.ts for Angular cross-tree cleanup (Task 2 unblock, 2026-05-17, this agent)
```

### Task 2 acceptance gates

```
[PASS]    /tmp/vr-regen-07.3.2-05-task2-attempt4.log shows "6/6 target sub-builds complete" inside container
[PASS]    Vue baseline regen run: "12 passed (10.3s)" inside container
[PASS]    Full matrix verify: "67 passed (27.6s)" + 5 D-04 functional failures (surfaced, in F-07.3.2-05-A) + 7 skipped (close-propagation gating)
[VERIFIED-CLEAN]  git diff tests/visual-regression/__screenshots__/ EMPTY post-Docker (zero baseline drift to commit — existing baselines correctly Linux-rendered)
[PASS]    Host bindings post-Docker restored via `CI=true pnpm install --frozen-lockfile` (turbo run build --filter @rozie/visual-regression --force GREEN 13/13 tasks after restore)
[PASS]    Docker recipe used the pinned `mcr.microsoft.com/playwright:v1.60.0-jammy` image per DEBUG.md:283-321 (with `--memory=8g` flag added to harden against transient ENOMEM)
```

**Self-check conclusion:** Plan 05 status: **COMPLETE within scope.** Task 1 spec extension landed; Task 2 PNG baseline verification confirms zero drift. Producer-side D-04 functional regressions surfaced for a follow-up plan (F-07.3.2-05-A) — out of Plan 05's scope per Rule 4.
