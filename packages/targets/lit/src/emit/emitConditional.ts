/**
 * emitConditional.ts — P1 stub for r-if/r-else-if/r-else lit-html emission.
 *
 * P2 emits an inline ternary chain inside `html\`${cond ? html\`...\` : nothing}\``,
 * using lit-html's `nothing` sentinel for empty branches.
 *
 * @experimental — shape may change before v1.0
 */

export function emitConditional(): string {
  return '';
}
