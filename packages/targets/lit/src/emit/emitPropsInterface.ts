/**
 * emitPropsInterface.ts — P1 stub for `<props>` decorator emission.
 *
 * P2 emits per-prop `@property({ type: Type, reflect: D-LIT-04-policy }) name;`
 * class fields. Unlike React's standalone `interface FooProps {...}`, Lit's
 * prop types are declarator-attached.
 *
 * @experimental — shape may change before v1.0
 */

export function emitPropsInterface(): string {
  return '';
}
