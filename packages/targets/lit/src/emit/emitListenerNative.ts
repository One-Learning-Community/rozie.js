/**
 * emitListenerNative.ts — P1 stub for native event-listener emission.
 *
 * P2 emits `host.addEventListener(event, handler, options)` paired with
 * cleanup pushed to `this._disconnectCleanups`.
 *
 * @experimental — shape may change before v1.0
 */

export function emitListenerNative(): string {
  return '';
}
