/**
 * emitHostListenerWiring.ts — P1 stub for host-element (this) event wiring.
 *
 * P2 emits the per-host-event setup that complements emitListeners — typically
 * `this.addEventListener(event, handler, options)` for listeners whose `target`
 * is the host element rather than `document` / `window`.
 *
 * @experimental — shape may change before v1.0
 */

export function emitHostListenerWiring(): string {
  return '';
}
