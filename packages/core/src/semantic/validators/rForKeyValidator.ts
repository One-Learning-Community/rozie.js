/**
 * SEM-03 / ROZ300/ROZ301/ROZ302 — r-for :key hygiene validator (STUB; Plan 02-02 Task 3 lands the implementation).
 *
 * Plan 02-02 Task 1 lays this no-op stub so analyze.ts typechecks cleanly
 * before Task 3 runs.
 *
 * Per D-08 collected-not-thrown: NEVER throws.
 */
import type { RozieAST } from '../../ast/types.js';
import type { Diagnostic } from '../../diagnostics/Diagnostic.js';

export function runRForKeyValidator(
  _ast: RozieAST,
  _diagnostics: Diagnostic[],
): void {
  // No-op until Task 3 lands the implementation.
}
