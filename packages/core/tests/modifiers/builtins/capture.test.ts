// Wave 0 scaffold (Plan 02-01 Task 4) — Plan 02-04 fills these in.
//
// MOD-04 .capture — addEventListener({ capture: true }).
import { describe, it } from 'vitest';

describe('builtin: .capture — Plan 02-04', () => {
  it.todo('.capture → ModifierPipelineEntry { kind: "listenerOption", option: "capture" }');
});
