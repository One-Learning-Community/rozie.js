// Wave 0 scaffold (Plan 02-01 Task 4) — Plan 02-04 fills these in.
//
// MOD-04 .debounce(ms) — wraps the handler in a setTimeout-based debouncer.
import { describe, it } from 'vitest';

describe('builtin: .debounce — Plan 02-04', () => {
  it.todo('.debounce(300) → ModifierPipelineEntry { kind: "wrap", modifier: "debounce", args: [literal:300] }');
  it.todo('.debounce() → ROZ111 arity mismatch (requires exactly 1 numeric arg)');
  it.todo('.debounce(300, 500) → ROZ111 arity mismatch (too many args)');
});
