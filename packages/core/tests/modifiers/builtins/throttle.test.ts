// Wave 0 scaffold (Plan 02-01 Task 4) — Plan 02-04 fills these in.
//
// MOD-04 .throttle(ms) — wraps the handler with a throttle window.
import { describe, it } from 'vitest';

describe('builtin: .throttle — Plan 02-04', () => {
  it.todo('.throttle(100) → ModifierPipelineEntry { kind: "wrap", modifier: "throttle", args: [literal:100] }');
  it.todo('.throttle() → ROZ111 arity mismatch (requires exactly 1 numeric arg)');
});
