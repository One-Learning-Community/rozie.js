// Wave 0 scaffold (Plan 02-01 Task 4) — Plan 02-04 fills these in.
//
// MOD-04 .prevent — calls event.preventDefault() before the handler.
import { describe, it } from 'vitest';

describe('builtin: .prevent — Plan 02-04', () => {
  it.todo('.prevent → ModifierPipelineEntry { kind: "filter", modifier: "prevent", args: [] }');
});
