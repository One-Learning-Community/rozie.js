// Wave 0 scaffold (Plan 02-01 Task 4) — Plan 02-04 fills these in.
//
// MOD-04 .self — fires only when event.target === event.currentTarget
// (i.e., event originated on the bound element itself, not a descendant).
import { describe, it } from 'vitest';

describe('builtin: .self — Plan 02-04', () => {
  it.todo('.self → ModifierPipelineEntry { kind: "filter", modifier: "self", args: [] }');
  it.todo('.self(arg) → ROZ111 arity mismatch (no args expected)');
});
