export { default as DataTable } from './DataTable';
export { default } from './DataTable';
export { default as Column } from './Column';

/** The `$expose` imperative handle received via `ref` — { sortColumn, clearSorting, getColumnDefs, toggleAllRows, clearSelection, getSelectedRows, setPage, setRowsPerPage, toggleColumnVisibility, setColumnOrder, resetColumnSizing, pinColumn }. */
export type { DataTableHandle } from './DataTable';
