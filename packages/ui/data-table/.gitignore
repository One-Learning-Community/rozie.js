# Wave-0 probe build artifacts (throwaway; regenerated by scripts/probe-48-00.mjs)
scripts/probe-48/dist/
scripts/probe-48/*.rozie.ts
scripts/probe-48/*.rozie.js
# The per-framework leaves under packages/<fw>/ ARE committed (the slider/rete
# precedent — codegen-generated src is source-controlled so the published
# packages ship without a build step). Only build/runtime artifacts are ignored.
packages/*/dist/
packages/*/.turbo/
dist/
node_modules/
