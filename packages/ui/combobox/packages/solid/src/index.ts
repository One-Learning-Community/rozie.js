export { default as Combobox } from './Combobox';
export { default } from './Combobox';

/** The `$expose` imperative handle received via `ref` — { focus, clear, seedQuery, pinOpen }. */
export type { ComboboxHandle } from './Combobox';
