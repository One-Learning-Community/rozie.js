export { default as Pagination } from './Pagination';
export { default } from './Pagination';

/** The `$expose` imperative handle received via `ref` — { goto, next, prev, first, last }. */
export type { PaginationHandle } from './Pagination';
