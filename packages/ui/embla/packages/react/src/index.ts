export { default as Carousel } from './Carousel';
export { default } from './Carousel';

/** The `$expose` imperative handle received via `ref` — { scrollNext, scrollPrev, scrollToIndex, reInitCarousel, canScrollNext, canScrollPrev, getSelectedIndex, scrollSnapList, scrollProgress, slidesInView, slidesNotInView, previousScrollSnap, getPlugins, getInstance }. */
export type { CarouselHandle } from './Carousel';
