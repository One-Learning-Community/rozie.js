export { default as FullCalendar } from './FullCalendar';
export { default } from './FullCalendar';

/** The `$expose` imperative handle received via `ref` — { getApi, changeView, addEvent, removeEvent, today, prev, next, gotoDate, getDate, getEvents, scrollToTime, updateSize, prevYear, nextYear, selectRange, clearSelection }. */
export type { FullCalendarHandle } from './FullCalendar';
