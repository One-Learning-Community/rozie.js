export { default as Slider } from './Slider';
export { default } from './Slider';

/** The `$expose` imperative handle received via `ref` — { focus, increment, decrement }. */
export type { SliderHandle } from './Slider';
