export { default as Listbox } from './Listbox';
export { default } from './Listbox';

/** The `$expose` imperative handle received via `ref` — { open, close, toggle, clear, focusControl }. */
export type { ListboxHandle } from './Listbox';
