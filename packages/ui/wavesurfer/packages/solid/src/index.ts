export { default as Waveform } from './Waveform';
export { default } from './Waveform';

/** The `$expose` imperative handle received via `ref` — { play, pause, playPause, stop, seekTo, setTime, setVolume, setPlaybackRate, setZoom, load, isPlaying, getDuration, getCurrentTime, getWaveSurfer, addRegion, clearRegions, getRegions }. */
export type { WaveformHandle } from './Waveform';
