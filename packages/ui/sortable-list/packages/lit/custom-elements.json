{
  "schemaVersion": "2.1.0",
  "readme": "",
  "modules": [
    {
      "kind": "javascript-module",
      "path": "dist/index.mjs",
      "declarations": [
        {
          "kind": "class",
          "description": "Idiomatic Lit `<rozie-sortable-list>` — a cross-framework drag-and-drop list compiled from one Rozie source via SortableJS. Two-way `items` (with an `items-change` event), keyboard reordering, cross-list drag, and a 4-verb handle.",
          "name": "SortableList",
          "tagName": "rozie-sortable-list",
          "customElement": true,
          "superclass": {
            "name": "LitElement",
            "package": "lit"
          },
          "members": [
            {
              "kind": "field",
              "name": "items",
              "description": "The bound items array. The sole `model: true` prop — two-way bind it (`r-model:items` / `v-model:items` / `bind:items` / `[(items)]`) and SortableList writes the re-ordered array back whenever a drag, cross-list move, or keyboard reorder commits, with no manual `onChange → setState` wiring.",
              "type": {
                "text": "any[]"
              },
              "attribute": "items",
              "default": "[]"
            },
            {
              "kind": "field",
              "name": "itemKey",
              "description": "The per-row key the framework reconciler tracks each item by across a reorder — either a property name (e.g. `itemKey=\"id\"` reads `item.id`) or an `(item, index) => key` function. With neither, id-less object items get a stable synthetic key via an internal `WeakMap` (survives reorder by object identity); primitive items fall back to index — pass a function for reorderable duplicate primitives.",
              "type": {
                "text": "string | ((...args: any[]) => any)"
              },
              "attribute": "itemkey",
              "default": "null"
            },
            {
              "kind": "field",
              "name": "handle",
              "description": "CSS selector identifying the per-row drag handle, so a drag starts only from that element rather than anywhere in the row. Authored class names render literally on every target (React included), so a plain `.grip` works; `$classSelector('grip')` is an optional, typo-checked way to author it.",
              "type": {
                "text": "string"
              },
              "attribute": "handle",
              "default": "null"
            },
            {
              "kind": "field",
              "name": "group",
              "description": "SortableJS group name enabling cross-list drag — two lists sharing a `group` accept items between each other (the source fires `remove`, the destination fires `add`). Set `cloneable: true` to flip a string group into clone-mode.",
              "type": {
                "text": "string"
              },
              "attribute": "group",
              "default": "null"
            },
            {
              "kind": "field",
              "name": "animation",
              "description": "Reorder animation duration in milliseconds. `0` disables the animation. Runtime-updatable.",
              "type": {
                "text": "number"
              },
              "attribute": "animation",
              "default": "150"
            },
            {
              "kind": "field",
              "name": "disabled",
              "description": "Temporarily disable dragging without unmounting — reapplied live via `instance.option('disabled', v)` (no remount). Also suppresses keyboard reordering: a disabled list is not sortable by any input, so rows lose their `tabindex` and the keydown handler no-ops.",
              "type": {
                "text": "boolean"
              },
              "attribute": "disabled",
              "default": "false"
            },
            {
              "kind": "field",
              "name": "disableKeyboard",
              "description": "Opt out of keyboard reordering (Space lift / Arrow move / Esc cancel / Enter drop) while leaving pointer drag enabled. Rows drop out of the tab order (no `tabindex`) and the keydown handler no-ops. Keyboard access is gated on `!disabled && !disableKeyboard`.",
              "type": {
                "text": "boolean"
              },
              "attribute": "disablekeyboard",
              "default": "false"
            },
            {
              "kind": "field",
              "name": "options",
              "description": "Verbatim SortableJS options pass-through for anything not covered by the named props. The named props win on key conflict but `options` lands AFTER them in the merge so consumers can override defaults; handler keys (`onStart`, `onEnd`, `onUpdate`, `onAdd`, `onRemove`, `onClone`) are stripped — the helper owns those paths.",
              "type": {
                "text": "Record<string, any>"
              },
              "attribute": "options",
              "default": "{}"
            },
            {
              "kind": "field",
              "name": "labelFor",
              "description": "Optional `(item, idx) => string` returning the screen-reader label for the aria-live announcer during keyboard drag. Defaults to `item.label` (or `String(item)` when no `label` field exists).",
              "type": {
                "text": "((...args: any[]) => any)"
              },
              "attribute": "labelfor",
              "default": "null"
            },
            {
              "kind": "field",
              "name": "ghostClass",
              "description": "Class name applied to the drop-placeholder (ghost) element while dragging. Forwarded live via `instance.option`, so toggling it at runtime takes effect without a remount.",
              "type": {
                "text": "string"
              },
              "attribute": "ghostclass",
              "default": "null"
            },
            {
              "kind": "field",
              "name": "chosenClass",
              "description": "Class name applied to the currently-chosen item while dragging. Forwarded live via `instance.option` (no remount needed to change it).",
              "type": {
                "text": "string"
              },
              "attribute": "chosenclass",
              "default": "null"
            },
            {
              "kind": "field",
              "name": "dragClass",
              "description": "Class name applied to the dragging element. Only takes effect in fallback mode (`forceFallback: true`). Forwarded live via `instance.option`.",
              "type": {
                "text": "string"
              },
              "attribute": "dragclass",
              "default": "null"
            },
            {
              "kind": "field",
              "name": "filter",
              "description": "CSS selector that prevents drag initiation on matching rows (locked items). SortableJS checks it at `mousedown`/`touchstart` and aborts the drag if it matches. A `data-*` attribute selector (e.g. `[data-locked]`) is the most robust choice across all targets.",
              "type": {
                "text": "string"
              },
              "attribute": "filter",
              "default": "null"
            },
            {
              "kind": "field",
              "name": "easing",
              "description": "CSS easing function for the reorder animation (e.g. `'ease-in'`, `'cubic-bezier(0.4, 0, 0.2, 1)'`). Runtime-updatable.",
              "type": {
                "text": "string"
              },
              "attribute": "easing",
              "default": "null"
            },
            {
              "kind": "field",
              "name": "forceFallback",
              "description": "Force SortableJS's mouse-event drag path over HTML5 DnD — useful for touch devices, consistent cross-browser behavior, and synthetic test drivers (and `dragClass` only applies in this mode). **Construction-time only**: SortableJS reads it once at construction, so re-key the `<SortableList>` to toggle it at runtime.",
              "type": {
                "text": "boolean"
              },
              "attribute": "forcefallback",
              "default": "false"
            },
            {
              "kind": "field",
              "name": "swapThreshold",
              "description": "SortableJS swap threshold (0..1) — a lower value makes rows swap earlier as the dragged item overlaps a neighbor. Reapplied live via `instance.option('swapThreshold', v)` — SortableJS reads it on every dragover, so no remount is needed.",
              "type": {
                "text": "number"
              },
              "attribute": "swapthreshold",
              "default": "1"
            },
            {
              "kind": "field",
              "name": "cloneable",
              "description": "High-level prop that REPLACES a string `group` with SortableJS's `{ name, pull: 'clone', put: true }` clone-mode object form — the source deposits a COPY onto the destination and keeps its own array unchanged (the palette → canvas pattern). With `group: null` it is a no-op (a clone-mode list with no group name has no peer to clone into). Reapplied live — toggling `cloneable` (or changing `group`) recomputes the clone-mode shape and reapplies it via `instance.option('group', …)`, no remount.",
              "type": {
                "text": "boolean"
              },
              "attribute": "cloneable",
              "default": "false"
            },
            {
              "kind": "field",
              "name": "listClass",
              "description": "Extra class(es) merged onto the list container (the SortableJS root) alongside the base `rozie-sortable-list` class. Accepts a `String`, `Array`, or `Object` (Vue-style class binding), normalized identically across all six targets — the hook for bridging a CSS framework (`.list-group`) or a flex/grid parent onto the component.",
              "type": {
                "text": "string | any[] | Record<string, any>"
              },
              "attribute": "listclass",
              "default": "\"\""
            },
            {
              "kind": "field",
              "name": "itemClass",
              "description": "Extra class(es) merged onto every item row alongside the base `rozie-sortable-item` class. Accepts a `String`, `Array`, or `Object` (Vue-style class binding) applied uniformly, OR an `(item, index) => class` function for per-row classes evaluated at render time. Normalized identically across all six targets.",
              "type": {
                "text": "string | any[] | Record<string, any> | ((...args: any[]) => any)"
              },
              "attribute": "itemclass",
              "default": "\"\""
            },
            {
              "kind": "field",
              "name": "itemStyle",
              "description": "Per-row inline style applied to the `.rozie-sortable-item` wrapper. Accepts a CSS `String`, a flat style object (`Record<string, string | number>`), or an `(item, index) => string | object` function for per-row styling. Because it lands on the wrapper — the direct child of the list container — it can drive CSS-grid placement (`grid-column` / `grid-row` / `align-self`) when `listClass` sets `display: grid`. Normalized per target; `null` / empty drops the attribute.",
              "type": {
                "text": "string | Record<string, any> | ((...args: any[]) => any)"
              },
              "attribute": "itemstyle",
              "default": "null"
            },
            {
              "kind": "method",
              "name": "getInstance",
              "description": "Return the underlying SortableJS instance for direct API access (the raw-engine escape hatch — `save`, `closest`, etc. are one hop away). `null` before mount and after destroy."
            },
            {
              "kind": "method",
              "name": "toArray",
              "description": "Return the current order as an array of `data-id` strings (each row carries `data-id=\"<key>\"`). `[]` before mount."
            },
            {
              "kind": "method",
              "name": "sort",
              "description": "Reorder the list by an array of `data-id` strings — `sort(order, useAnimation = true)`."
            },
            {
              "kind": "method",
              "name": "option",
              "description": "Read or set a live SortableJS option — `option(name)` gets, `option(name, value)` sets. The runtime escape hatch for options beyond the curated props."
            }
          ],
          "attributes": [
            {
              "name": "items",
              "description": "The bound items array. The sole `model: true` prop — two-way bind it (`r-model:items` / `v-model:items` / `bind:items` / `[(items)]`) and SortableList writes the re-ordered array back whenever a drag, cross-list move, or keyboard reorder commits, with no manual `onChange → setState` wiring.",
              "type": {
                "text": "any[]"
              },
              "fieldName": "items",
              "default": "[]"
            },
            {
              "name": "itemkey",
              "description": "The per-row key the framework reconciler tracks each item by across a reorder — either a property name (e.g. `itemKey=\"id\"` reads `item.id`) or an `(item, index) => key` function. With neither, id-less object items get a stable synthetic key via an internal `WeakMap` (survives reorder by object identity); primitive items fall back to index — pass a function for reorderable duplicate primitives.",
              "type": {
                "text": "string | ((...args: any[]) => any)"
              },
              "fieldName": "itemKey",
              "default": "null"
            },
            {
              "name": "handle",
              "description": "CSS selector identifying the per-row drag handle, so a drag starts only from that element rather than anywhere in the row. Authored class names render literally on every target (React included), so a plain `.grip` works; `$classSelector('grip')` is an optional, typo-checked way to author it.",
              "type": {
                "text": "string"
              },
              "fieldName": "handle",
              "default": "null"
            },
            {
              "name": "group",
              "description": "SortableJS group name enabling cross-list drag — two lists sharing a `group` accept items between each other (the source fires `remove`, the destination fires `add`). Set `cloneable: true` to flip a string group into clone-mode.",
              "type": {
                "text": "string"
              },
              "fieldName": "group",
              "default": "null"
            },
            {
              "name": "animation",
              "description": "Reorder animation duration in milliseconds. `0` disables the animation. Runtime-updatable.",
              "type": {
                "text": "number"
              },
              "fieldName": "animation",
              "default": "150"
            },
            {
              "name": "disabled",
              "description": "Temporarily disable dragging without unmounting — reapplied live via `instance.option('disabled', v)` (no remount). Also suppresses keyboard reordering: a disabled list is not sortable by any input, so rows lose their `tabindex` and the keydown handler no-ops.",
              "type": {
                "text": "boolean"
              },
              "fieldName": "disabled",
              "default": "false"
            },
            {
              "name": "disablekeyboard",
              "description": "Opt out of keyboard reordering (Space lift / Arrow move / Esc cancel / Enter drop) while leaving pointer drag enabled. Rows drop out of the tab order (no `tabindex`) and the keydown handler no-ops. Keyboard access is gated on `!disabled && !disableKeyboard`.",
              "type": {
                "text": "boolean"
              },
              "fieldName": "disableKeyboard",
              "default": "false"
            },
            {
              "name": "options",
              "description": "Verbatim SortableJS options pass-through for anything not covered by the named props. The named props win on key conflict but `options` lands AFTER them in the merge so consumers can override defaults; handler keys (`onStart`, `onEnd`, `onUpdate`, `onAdd`, `onRemove`, `onClone`) are stripped — the helper owns those paths.",
              "type": {
                "text": "Record<string, any>"
              },
              "fieldName": "options",
              "default": "{}"
            },
            {
              "name": "labelfor",
              "description": "Optional `(item, idx) => string` returning the screen-reader label for the aria-live announcer during keyboard drag. Defaults to `item.label` (or `String(item)` when no `label` field exists).",
              "type": {
                "text": "((...args: any[]) => any)"
              },
              "fieldName": "labelFor",
              "default": "null"
            },
            {
              "name": "ghostclass",
              "description": "Class name applied to the drop-placeholder (ghost) element while dragging. Forwarded live via `instance.option`, so toggling it at runtime takes effect without a remount.",
              "type": {
                "text": "string"
              },
              "fieldName": "ghostClass",
              "default": "null"
            },
            {
              "name": "chosenclass",
              "description": "Class name applied to the currently-chosen item while dragging. Forwarded live via `instance.option` (no remount needed to change it).",
              "type": {
                "text": "string"
              },
              "fieldName": "chosenClass",
              "default": "null"
            },
            {
              "name": "dragclass",
              "description": "Class name applied to the dragging element. Only takes effect in fallback mode (`forceFallback: true`). Forwarded live via `instance.option`.",
              "type": {
                "text": "string"
              },
              "fieldName": "dragClass",
              "default": "null"
            },
            {
              "name": "filter",
              "description": "CSS selector that prevents drag initiation on matching rows (locked items). SortableJS checks it at `mousedown`/`touchstart` and aborts the drag if it matches. A `data-*` attribute selector (e.g. `[data-locked]`) is the most robust choice across all targets.",
              "type": {
                "text": "string"
              },
              "fieldName": "filter",
              "default": "null"
            },
            {
              "name": "easing",
              "description": "CSS easing function for the reorder animation (e.g. `'ease-in'`, `'cubic-bezier(0.4, 0, 0.2, 1)'`). Runtime-updatable.",
              "type": {
                "text": "string"
              },
              "fieldName": "easing",
              "default": "null"
            },
            {
              "name": "forcefallback",
              "description": "Force SortableJS's mouse-event drag path over HTML5 DnD — useful for touch devices, consistent cross-browser behavior, and synthetic test drivers (and `dragClass` only applies in this mode). **Construction-time only**: SortableJS reads it once at construction, so re-key the `<SortableList>` to toggle it at runtime.",
              "type": {
                "text": "boolean"
              },
              "fieldName": "forceFallback",
              "default": "false"
            },
            {
              "name": "swapthreshold",
              "description": "SortableJS swap threshold (0..1) — a lower value makes rows swap earlier as the dragged item overlaps a neighbor. Reapplied live via `instance.option('swapThreshold', v)` — SortableJS reads it on every dragover, so no remount is needed.",
              "type": {
                "text": "number"
              },
              "fieldName": "swapThreshold",
              "default": "1"
            },
            {
              "name": "cloneable",
              "description": "High-level prop that REPLACES a string `group` with SortableJS's `{ name, pull: 'clone', put: true }` clone-mode object form — the source deposits a COPY onto the destination and keeps its own array unchanged (the palette → canvas pattern). With `group: null` it is a no-op (a clone-mode list with no group name has no peer to clone into). Reapplied live — toggling `cloneable` (or changing `group`) recomputes the clone-mode shape and reapplies it via `instance.option('group', …)`, no remount.",
              "type": {
                "text": "boolean"
              },
              "fieldName": "cloneable",
              "default": "false"
            },
            {
              "name": "listclass",
              "description": "Extra class(es) merged onto the list container (the SortableJS root) alongside the base `rozie-sortable-list` class. Accepts a `String`, `Array`, or `Object` (Vue-style class binding), normalized identically across all six targets — the hook for bridging a CSS framework (`.list-group`) or a flex/grid parent onto the component.",
              "type": {
                "text": "string | any[] | Record<string, any>"
              },
              "fieldName": "listClass",
              "default": "\"\""
            },
            {
              "name": "itemclass",
              "description": "Extra class(es) merged onto every item row alongside the base `rozie-sortable-item` class. Accepts a `String`, `Array`, or `Object` (Vue-style class binding) applied uniformly, OR an `(item, index) => class` function for per-row classes evaluated at render time. Normalized identically across all six targets.",
              "type": {
                "text": "string | any[] | Record<string, any> | ((...args: any[]) => any)"
              },
              "fieldName": "itemClass",
              "default": "\"\""
            },
            {
              "name": "itemstyle",
              "description": "Per-row inline style applied to the `.rozie-sortable-item` wrapper. Accepts a CSS `String`, a flat style object (`Record<string, string | number>`), or an `(item, index) => string | object` function for per-row styling. Because it lands on the wrapper — the direct child of the list container — it can drive CSS-grid placement (`grid-column` / `grid-row` / `align-self`) when `listClass` sets `display: grid`. Normalized per target; `null` / empty drops the attribute.",
              "type": {
                "text": "string | Record<string, any> | ((...args: any[]) => any)"
              },
              "fieldName": "itemStyle",
              "default": "null"
            }
          ],
          "events": [
            {
              "name": "change",
              "type": {
                "text": "CustomEvent"
              },
              "description": "Fired after the list order changes (same-list reorder commit)."
            },
            {
              "name": "add",
              "type": {
                "text": "CustomEvent"
              },
              "description": "Fired when an item is added from another list (cross-list destination commit)."
            },
            {
              "name": "remove",
              "type": {
                "text": "CustomEvent"
              },
              "description": "Fired when an item is moved out to another list (cross-list source commit; not fired in clone mode)."
            },
            {
              "name": "start",
              "type": {
                "text": "CustomEvent"
              },
              "description": "Fired when dragging starts."
            },
            {
              "name": "end",
              "type": {
                "text": "CustomEvent"
              },
              "description": "Fired when dragging ends (source side)."
            },
            {
              "name": "items-change",
              "type": {
                "text": "CustomEvent"
              },
              "description": "Fired with the updated `items` (`event.detail`) when it changes."
            }
          ],
          "slots": [
            {
              "name": "header"
            },
            {
              "name": "",
              "description": "Scoped slot — props: item, index."
            },
            {
              "name": "footer"
            }
          ]
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "SortableList",
          "declaration": {
            "name": "SortableList",
            "module": "dist/index.mjs"
          }
        },
        {
          "kind": "js",
          "name": "default",
          "declaration": {
            "name": "SortableList",
            "module": "dist/index.mjs"
          }
        },
        {
          "kind": "custom-element-definition",
          "name": "rozie-sortable-list",
          "declaration": {
            "name": "SortableList",
            "module": "dist/index.mjs"
          }
        }
      ]
    }
  ]
}
