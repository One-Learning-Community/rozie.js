{
  // strictNullChecks/exactOptionalPropertyTypes/noImplicitAny relaxed: the
  // emitted Solid output is bundler-built (tsdown/oxc), not strict-tsc-authored.
  // Emitted DOM refs (typed string|null) and generated IIFE defaults are
  // proven-correct via dist-parity but are not author-strictness-clean.
  //
  // No `paths` alias for `@tiptap/*`: the published @tiptap packages ship real
  // types.
  "extends": "../../../../../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "module": "ESNext",
    "target": "ES2022",
    "moduleResolution": "bundler",
    "strict": true,
    "verbatimModuleSyntax": true,
    "jsx": "preserve",
    "jsxImportSource": "solid-js",
    "skipLibCheck": true,
    "exactOptionalPropertyTypes": false,
    "noImplicitAny": false,
    "lib": ["ES2022", "DOM", "DOM.Iterable"]
  },
  "include": ["src"],
  "exclude": ["dist", "node_modules"]
}
