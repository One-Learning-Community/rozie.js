export { default as TipTap } from './TipTap';
export { default } from './TipTap';

/** The `$expose` imperative handle received via `ref` — { getEditor, focusEditor, blurEditor, getHTML, getJSON, getText, setContent, clearContent, toggleBold, toggleItalic, toggleHeading, toggleBulletList, undo, redo, chain, isActive, can, isEmpty }. */
export type { TipTapHandle } from './TipTap';
