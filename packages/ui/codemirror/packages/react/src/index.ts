export { default as CodeMirror } from './CodeMirror';
export { default } from './CodeMirror';

/** The `$expose` imperative handle received via `ref` — { getView, focus, getValue, replaceValue, dispatch, insertText, getSelection, setSelection, undo, redo, selectAll, scrollToPos }. */
export type { CodeMirrorHandle } from './CodeMirror';
