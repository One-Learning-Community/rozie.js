export { default as Cropper } from './Cropper';
export { default } from './Cropper';

/** The `$expose` imperative handle received via `ref` — { getCropper, getData, getCroppedCanvas, getCroppedDataURL, reset, clear, showCropBox, replace, rotateTo, rotateBy, zoomTo, zoomBy, scaleX, scaleY, enable, disable, setAspectRatio, setDragMode }. */
export type { CropperHandle } from './Cropper';
