export { default as Cropper } from './Cropper';
export { default } from './Cropper';

/** The `$expose` imperative handle received via `ref` — { getCropper, getData, getCanvasData, getCropBoxData, getImageData, getContainerData, getCroppedCanvas, getCroppedDataURL, reset, clear, showCropBox, replace, rotateTo, rotateBy, zoomTo, zoomBy, scaleX, scaleY, scale, setCanvasData, setCropBoxData, moveTo, move, enable, disable, setAspectRatio, setDragMode }. */
export type { CropperHandle } from './Cropper';
