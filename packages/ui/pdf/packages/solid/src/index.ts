export { default as PdfViewer } from './PdfViewer';
export { default } from './PdfViewer';

/** The `$expose` imperative handle received via `ref` — { getDocument, getPageCount, goToPage, nextPage, prevPage, setScale, zoomIn, zoomOut, fitWidth, fitPage, rotateCW, rotateCCW, download, getMetadata, getOutline, find, findNext, findPrev, clearFind }. */
export type { PdfViewerHandle } from './PdfViewer';
