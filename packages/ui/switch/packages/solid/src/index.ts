export { default as Switch } from './Switch';
export { default } from './Switch';

/** The `$expose` imperative handle received via `ref` — { focus, toggle }. */
export type { SwitchHandle } from './Switch';
