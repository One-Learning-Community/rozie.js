export { default as NumberField } from './NumberField';
export { default } from './NumberField';

/** The `$expose` imperative handle received via `ref` — { focus, increment, decrement, clear }. */
export type { NumberFieldHandle } from './NumberField';
