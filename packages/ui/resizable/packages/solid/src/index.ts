export { default as Resizable } from './Resizable';
export { default } from './Resizable';

/** The `$expose` imperative handle received via `ref` — { applySize, reset }. */
export type { ResizableHandle } from './Resizable';
