{
  "//": "This `paths` entry is a module-IDENTITY pin, not error suppression. `vite`: `packages/unplugin` dev-resolves `vite@8.1.0` while this harness runs `vite@6.4.2`, producing two unrelated `Plugin` types at `vitest.config.ts`; this is the type-level analog of the `resolve.dedupe` block `vitest.config.ts` already applies at runtime. The former `@rozie/core` pin here was REMOVED on 2026-08-19 (quick-260819-798): it existed because `packages/targets/*/src/emit*.ts` and `packages/unplugin/src/**` mixed a bare `@rozie/core` specifier with relative `../../../core/src/**` imports, so any consumer of `@rozie/unplugin`'s raw-`src` type exports saw two declarations of `ModifierRegistry`. Those 68 files were unmixed at the source (all-relative), so the pin is no longer needed and no future consumer needs to copy it forward. See Phase 80 deferred-items.md item 7.",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ES2022",
    "moduleResolution": "Bundler",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "noEmit": true,
    "useDefineForClassFields": false,
    "lib": ["ES2022", "DOM"],
    "types": ["vitest/globals", "node"],
    "baseUrl": ".",
    "paths": {
      "vite": ["./node_modules/vite"]
    }
  },
  "include": ["*.ts", "probe/**/*.ts", "hosts/**/*.ts"],
  "angularCompilerOptions": {
    "strictTemplates": false
  }
}
