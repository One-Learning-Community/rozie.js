{
  "//": "This `paths` entry is a module-IDENTITY pin, not error suppression. `vite`: `packages/unplugin` dev-resolves `vite@8.1.0` while this harness runs `vite@6.4.2`, producing two unrelated `Plugin` types at `vitest.config.ts`; this is the type-level analog of the `resolve.dedupe` block `vitest.config.ts` already applies at runtime. The former `@rozie/core` pin was REMOVED 2026-08-19 (quick-260819-9tc): `packages/targets/*/src/**` and `packages/unplugin/src/**` used to reach core's PUBLIC symbols through relative `../../core/src/**` paths while importing others via the bare specifier, so any consumer of `@rozie/unplugin`'s raw-`src` type exports saw two nominal identities for `ModifierRegistry`/`IRCache`. Every import of a symbol the core barrel re-exports is now bare, so no consumer needs this pin any more. See Phase 80 deferred-items.md item 7.",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ES2022",
    "moduleResolution": "Bundler",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "noEmit": true,
    "useDefineForClassFields": false,
    "lib": ["ES2022", "DOM"],
    "types": ["vitest/globals", "node"],
    "baseUrl": ".",
    "paths": {
      "vite": ["./node_modules/vite"]
    }
  },
  "include": ["*.ts", "probe/**/*.ts", "hosts/**/*.ts"],
  "angularCompilerOptions": {
    "strictTemplates": false
  }
}
